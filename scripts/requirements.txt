# ============================================================================
# Dépendances Python pour les scripts de déploiement Azure ML
# ============================================================================
# Installation: pip install -r requirements.txt
# ============================================================================

# ===== Azure ML & Identity =====
azure-ai-ml>=1.8.0          # Azure ML SDK v2 (MLClient)
azure-identity>=1.11.0      # Authentification Azure
azure-common>=1.1.0         # Utilities Azure
azure-core>=1.26.0          # Core Azure SDK

# ===== Data Processing =====
pandas>=1.3.0               # Données
numpy>=1.21.0               # Calculs numériques

# ===== Machine Learning =====
scikit-learn>=1.0.0         # ML models
imbalanced-learn>=0.8.0     # SMOTE pour déséquilibre
xgboost>=1.5.0              # XGBoost models
mlflow>=2.12.1              # Tracking expériences

# ===== Azure ML MLflow Integration =====
azureml-mlflow>=1.50.0      # Intégration MLflow avec Azure ML

# ===== Configuration & Serialization =====
pyyaml>=6.0                 # Parsing YAML (endpoint.yml, deployment.yml)
pydantic>=1.9.0             # Validation schémas

# ===== Logging & Monitoring =====
python-dotenv>=0.20.0       # Variables d'environnement .env
applicationinsights>=0.11.0 # Application Insights

# ===== Optional: Development & Testing =====
pytest>=7.0.0               # Testing
pytest-asyncio>=0.20.0      # Tests async
black>=22.0.0               # Code formatting
flake8>=4.0.0               # Linting
mypy>=0.950                 # Type checking

# ============================================================================
# Notes de version:
# - azure-ai-ml: Minimum 1.8.0 requis pour les features récentes
# - azure-identity: DefaultAzureCredential support
# - mlflow: Compatible avec azureml-mlflow 1.50+
# - pyyaml: Pour parsing endpoint.yml et deployment.yml
# ============================================================================

# Commandes utiles:
# pip install -r requirements.txt               # Installation complète
# pip install -r requirements.txt --upgrade     # Mise à jour complète
# pip freeze > requirements.lock                # Exporter les versions exactes
# pip install -r requirements.lock              # Reproduire l'environnement exact
